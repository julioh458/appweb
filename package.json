{
  "scripts": {
    "build": "rm -rf public && mkdir public && echo 'Custom Build' > ./public/in.txt"
  },
  "dependencies": {
    "cookie-parser": "^1.4.5",
    "cors": "^2.8.5",
    "ejs": "^3.1.6",
    "express": "^4.17.1",
    "google-auth-library": "^7.9.1"
  }
}
